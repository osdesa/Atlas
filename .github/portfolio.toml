title = "Atlas"

summary = "Atlas is a tested C++20 task-graph prototype with sequential dependency-ordered execution."

status = "Active"

technologies = [
  "C++",
  "Vulkan SDK",
  "Catch2",
  "CMake",
  "Doxygen",
]

highlights = [
  "Built directed acyclic task graphs with dependency validation and cycle rejection.",
  "Implemented deterministic sequential execution using Kahn's algorithm with exception and timing results.",
  "Configured Vulkan SDK discovery as groundwork for a future compute backend; no Vulkan workloads execute yet.",
]

show_repository = true
include_archived = false
include_fork = false
