title = "Atlas"

summary = "Atlas is a tested C++20 task-graph prototype with dependency-aware concurrent CPU execution."

status = "Active"

technologies = [
  "C++",
  "Vulkan SDK",
  "Catch2",
  "CMake",
  "ThreadSanitizer",
  "Doxygen",
]

highlights = [
  "Built directed acyclic task graphs with dependency validation and cycle rejection.",
  "Implemented capacity-aware Kahn scheduling through interchangeable synchronous and fixed-size worker-pool CPU executors.",
  "Added attributed completions, exception isolation, draining shutdown, and deterministic concurrent graph tests.",
  "Configured Vulkan SDK discovery as groundwork for a future compute backend; no Vulkan workloads execute yet.",
]

show_repository = true
include_archived = false
include_fork = false
