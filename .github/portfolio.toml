title = "Atlas"

summary = "Atlas is a tested C++20 task graph with concurrent CPU and Vulkan compute execution."

status = "Active"

technologies = [
  "C++",
  "Vulkan SDK",
  "Catch2",
  "CMake",
  "ThreadSanitizer",
  "Doxygen",
]

highlights = [
  "Built directed acyclic task graphs with dependency validation and cycle rejection.",
  "Implemented capacity-aware Kahn scheduling through interchangeable synchronous and fixed-size worker-pool CPU executors.",
  "Built a compute-only Vulkan runtime with persistent buffers, pipelines, staging transfers, and declarative dispatches.",
  "Scheduled explicit CPU and GPU tasks through independent capacities and one shared attributed completion channel.",
  "Verified standalone and mixed compute through Mesa Lavapipe with checked GLSL-to-SPIR-V builds.",
  "Built a versioned reproducible benchmark runner with deterministic CPU/Vulkan DAGs and JSON Lines/CSV metrics.",
  "Compared direct and scheduled policy/slicing variants with paired deterministic confidence intervals.",
]

show_repository = true
include_archived = false
include_fork = false
